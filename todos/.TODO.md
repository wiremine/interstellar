# GAT-Based Step Trait Refactor

## Current Task: Implement Step trait with GATs

### Phase 1: Core Trait Definitions (step.rs)
- [ ] Add `#![feature(impl_trait_in_assoc_type)]` to lib.rs (if using nightly) OR use explicit iterator types
- [ ] Define new `Step` trait with GAT `type Iter<'a>`
- [ ] Define `DynStep` trait for type erasure
- [ ] Add blanket impl: `impl<S: Step> DynStep for S`
- [ ] Add `Clone` impl for `Box<dyn DynStep>`
- [ ] Migrate `IdentityStep` to new `Step` trait
- [ ] Migrate `StartStep` to new `Step` trait

### Phase 2: Update Helper Macros
- [ ] Update `impl_filter_step!` macro for `Step` trait
- [ ] Update `impl_flatmap_step!` macro for `Step` trait

### Phase 3: Migrate Transform Steps (metadata.rs)
- [ ] Migrate `IdStep`
- [ ] Migrate `LabelStep`
- [ ] Migrate `KeyStep`
- [ ] Migrate `ValueStep`
- [ ] Migrate `LoopsStep`
- [ ] Migrate `IndexStep`

### Phase 4: Update Pipeline
- [ ] Update `Traversal` to use `Box<dyn DynStep>`
- [ ] Update `execute_traversal` to use `DynStep`
- [ ] Update `execute_traversal_from` to use `DynStep`

### Phase 5: Migrate Remaining Steps
- [ ] Filter steps
- [ ] Navigation steps
- [ ] Branch steps

### Verification
- [ ] `cargo +nightly check` passes
- [ ] `cargo +nightly test` passes
