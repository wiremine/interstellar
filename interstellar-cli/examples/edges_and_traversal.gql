-- Edge creation and traversal with GQL
-- Run: interstellar query --memory --file examples/edges_and_traversal.gql

-- Create people with relationships inline
CREATE (:Person {name: 'Alice'})-[:knows {since: 2020}]->(:Person {name: 'Bob'});
CREATE (:Person {name: 'Charlie'});

-- Add more edges (need to match existing vertices and create edge)
-- For now, create another relationship inline
CREATE (:Person {name: 'Diana'})-[:knows {since: 2021}]->(:Person {name: 'Eve'});

-- Query: count vertices and edges
MATCH (n) RETURN count(n);
